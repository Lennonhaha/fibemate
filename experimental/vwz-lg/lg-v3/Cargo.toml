[package]
name = "lgv3"
version = "3.0.0-alpha"
edition = "2021"
description = "LG v3.0 — 置换群递归混淆引擎 (baseline: v2.2.2 + 模块化重构)"

[lib]
path = "src/lib.rs"
crate-type = ["cdylib", "staticlib", "lib"]

[dependencies]
wasm-bindgen = "0.2"
getrandom = { version = "0.2", features = ["js"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
