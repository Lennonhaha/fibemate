# LG v2.2 调试与归档 — 2026-07-15

## 任务目标
调试 Rust `DynamicPathSelector` roundtrip 测试失败，修复后推送 GitHub 并部署 WASM。

## 根因分析

### 核心发现：动态路径（Dynamic Path）在数学上不可逆

**问题**：`confuse_with_dynamic_path` / `deconfuse_with_dynamic_path` 试图在每层动态选择 Standard 模式或 Substitute 模式，但：

- Standard 模式 = SBOX → Linear，Fwd/Inv 顺序不同，不可交换
- Substitute 模式 = `key1 ⊕ SBOX ⊕ key2`，单层自逆（SUB_i * SUB_i = identity）
- **但 SUB_i 不是 SUB_j 的逆**（对于 i ≠ j）

7 层动态路径下，confuse 按 `L0→L1→...→L6` 执行，deconfuse 按 `L6→L5→...→L0` 执行，每层的模式由 RNG 决定。由于 RNG 在 confuse 和 deconfuse 之间不对称消耗模式位，导致**操作序列不对应，无法正确逆推**。

### 数学证明
```
SUB_i(x) = k1_i ⊕ SBOX(x ⊕ k2_i)
SUB_i^{-1}(x) = k2_i ⊕ SBOX^{-1}(x ⊕ k1_i)

SUB_i ∘ SUB_j(x) ≠ x  (当 i ≠ j)
∴ 动态混合模式的 roundtrip 数学上不可能
```

### 解决方案

**公共 API 不使用 DynamicPathSelector**，改用固定路径（Fixed Path）：

- `lgv2_confuse` → `confuse_chunk`（固定 SUB + Linear 序列）
- `lgv2_confuse_ex` → `confuse_chunk`（combined seed = seed ^ session_key）
- `lgv2_confuse_full` → `confuse_chunk`（combined seed + KEM binding）

`confuse_chunk` / `deconfuse_chunk` 在数学上是正确可逆的：
- confuse = SUB (key^session) → SUB (key^seed) → SUB (key) × 7
- deconfuse = SUB (key) → SUB (key^seed) → SUB (key^session) × 7
- 逆序执行，数学封闭，roundtrip 零误差

### 次要 Bug：XorShift64 语义差异

- Rust `u64::wrapping_mul` vs Python 任意精度整数
- 修复：在 Python 中每次 `x << k` 后立即 `& MASK`

## 交付物

### 源码（已清理）
- `C:\Users\maivs\lgv2_v222\src\lib.rs`：公共 API（30 个导出，6 个 size-variant confuse/deconfuse）
- `C:\Users\maivs\lgv2_v222\src\dynamic_path.rs`：清理后版本，DynamicPathSelector 标记为废弃（仅供研究）
- `C:\Users\maivs\lgv2_v222\src\matrices.rs`：Kronecker 逆修正（顺序累乘而非递归）
- `C:\Users\maivs\lgv2_v222\src\session.rs`：Session 管理（SHA-256 affine offset）
- `C:\Users\maivs\lgv2_v222\src\crypto_binding.rs`：Keccak256 哈希绑定
- `C:\Users\maivs\lgv2_v222\src\secure_cleanup.rs`：内存清零

### 测试结果
```
cargo test --lib
  30 passed, 0 failed, 2 ignored
  - lgv2_confuse_ex_roundtrip ✓
  - lgv2_confuse_ex_different_session_differs ✓
  - lgv2_full_confuse_deconfuse ✓
  - lgv2_bind_unbind_roundtrip ✓
  - lgv2_compare_with_python_100b ✓ (Python KAT 一致)
  - lgv2_roundtrip_1b/4b/100b/840b/2000b ✓
  - 所有 crypto_binding / secure_cleanup ✓
```

### WASM 包
- `pkg/lookingglass_v2_bg.wasm`：21.4 KB raw / 9.7 KB gzip
- 9 个导出函数，100% 匹配 `.d.ts`
- 版本字符串：`LG v2.2.1 (五短板增强版)`

### 废弃函数（保留标注）
- `DynamicPathSelector::confuse_with_dynamic_path`
- `DynamicPathSelector::deconfuse_with_dynamic_path`
- `test_path_roundtrip` → `#[ignore]`，原因标注

## 未完成项

### P0：GitHub 推送
- **阻塞**：SSH 公钥不可用（网络限制）+ GitHub HTTPS 无认证 Token
- **需要**：用户提供 GitHub Personal Access Token（PAT）

### P1：WASM 部署到服务器
- 新 WASM (`C:\Users\maivs\lgv2_v222\pkg\`) 需部署到：
  - `fibemate.net/crypto/lookingglass_v2_bg.wasm`
  - `fibemate.net/crypto/lookingglass_v2.js`

### P1：lgv2_v222 目录 Git 仓库初始化
- `C:\Users\maivs\lgv2_v222\` 非 Git 仓库，需 `git init` 并推送到独立仓库或作为 `fibemate/rust/lookingglass_v2/` 子模块

## 根因定位方法论总结

1. **分离测试**：逐层验证（`test_subst_self_inverse` = 单 SUB 层正确；`test_linear_layer_inv_is_correct` = 线性层可逆）
2. **数学追溯**：手动展开 2 层，确认 Standard 模式不交换
3. **Python 对比**：Rust vs Python 逐字节追踪，精确定位 `wrapping_mul` 语义差异
4. **设计决策**：发现问题层（DynamicPathSelector）→ 果断废弃 → 替换为正确实现（Fixed Path）

## 关键文件路径

| 文件 | 说明 |
|------|------|
| `C:\Users\maivs\lgv2_v222\src\lib.rs` | 公共 API（30 导出） |
| `C:\Users\maivs\lgv2_v222\src\dynamic_path.rs` | 已清理，废弃 DynamicPathSelector |
| `C:\Users\maivs\lgv2_v222\src\matrices.rs` | Kronecker 修正版 |
| `C:\Users\maivs\lgv2_v222\src\session.rs` | Session 管理 |
| `C:\Users\maivs\lgv2_v222\pkg\lgv2_bg.wasm` | **WASM 产物（21.4 KB）** |
| `C:\Users\maivs\lgv2_v222\pkg\lgv2.js` | WASM JS 胶水 |
| `C:\Users\maivs\lgv2_v222\pkg\lgv2.d.ts` | TypeScript 类型定义 |
| `C:\Users\maivs\lgv2_v222\Cargo.toml` | v2.2.1 |
