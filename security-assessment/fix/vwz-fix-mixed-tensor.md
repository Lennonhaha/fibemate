# VWZ 签名漏洞修复方案：混合 Vandermonde 张量

## 1. 攻击根因回顾

原实现的公钥张量每个切片均为 **rank-1**：

```
ψ[i1] = x1[i1] · (X2ᵀ·u[i1]) ⊗ (X3ᵀ·v[i1])
```

验签方程退化为可分离双线性式：

```
t[i1] = (u[i1]·w2) · (v[i1]·w3)
```

攻击者 `attack_vwz.py` 的利用链：
1. 从公钥每个切片提取 rank-1 因子 `u[i1], v[i1]`（无需 trapdoor）
2. 利用 target 的稀疏结构：恰 k 个零位（Z）、k+1 个非零位（S）
3. 解 Z 上的齐次系统 `u[i1]·w2 = 0`（k 个方程、k+1 个未知 → 非零解 w2）
4. 解 S 上的仿射系统 `v[i1]·w3 = t[i1]/(u[i1]·w2)`（k+1 个方程、k+1 个未知 → 唯一解 w3）

复杂度 O(k³)，36/36 伪造成功，且通过官方 WASM verify。

## 2. 修复方向

**核心思路**：破坏切片的 rank-1 可分离性，使验签方程不可分离，同时保持 trapdoor 采样可行。

**不可行方案（经验证）**：
- 参数放大（k 增大）：攻击多项式时间，无效
- 满秩随机切片：trapdoor 采样（固定 w3 解 w2）失败率 0/200，采样机制不可行
- 简单扰动：破坏验签精确性

**可行方案**：混合两个独立 Vandermonde 张量，使每个切片为 **rank-2**（两个 rank-1 外积之和）：

```
ψ[i1] = x1[i1] · [ (X2aᵀ·u_a[i1])⊗(X3aᵀ·v_a[i1])
                 + (X2bᵀ·u_b[i1])⊗(X3bᵀ·v_b[i1]) ]
```

其中 `u_a[i1]=(1,λa^1,...,λa^m)`, `v_a[i1]=(1,λc^1,...)`，`u_b[i1]=(1,λb^1,...)`, `v_b[i1]=(1,λc^1,...)`。

验签变为两项和，不可分离：

```
t[i1] = P2a(λa[i1])·P3a(λc[i1]) + P2b(λb[i1])·P3b(λc[i1])
```

**关键参数修正**：经验证 `n = m`（方阵）时攻击者固定 w2 解 w3 的线性系统可逆（20/20 可逆），可任意伪造。因此必须 **n > m**：

- `m = 2k+1`（w2/w3 维度，提供采样自由度）
- `n = 2k+2`（target 长度，大于 m 使固定-w2 线性系统超定无解）

## 3. Trapdoor 采样算法（新）

对稀疏 target（k+1 非零位 S，k+1 零位 Z）：

1. **u3 零化**：将 Z 划分为 Za、Zb，`|Za|+|Zb| = |Z|+1`（恰 1 个重叠位，保证 u2 约束恰好平衡）。求 u3 使：
   - `P3a(λc[i1]) = 0` for i1 ∈ Za（约束行 = vand(λc)）
   - `P3b(λc[i1]) = 0` for i1 ∈ Zb（约束行 = M3ᵀ·vand(λc)，其中 M3 = X3b·X3a⁻¹）
   - 零空间维数 = m - (|Za|+|Zb|) ≥ 1，随机组合非零解
2. **S 上 P3 非零**：从零空间随机组合 u3，要求所有 i1∈S 上 P3a、P3b ≠ 0
3. **u2 求解**：解线性系统
   - S 上：`P2a(λa)·P3a + P2b(λb)·P3b = target[i1]/x1[i1]`
   - Za 独有位（∈Za 且 ∉Zb）：`P2b(λb[i1]) = 0`（因 P3b 在此非零）
   - Zb 独有位（∈Zb 且 ∉Za）：`P2a(λa[i1]) = 0`
   - 共 k+1 + (独有位) 个方程，恰好 ≤ m，可解
4. `w2 = X2a⁻¹·u2`，`w3 = X3a⁻¹·u3`

**验签**：`public_eval(pk, w2, w3) == target`（协议不变）

## 4. 验证结果

### 4.1 合法签名验签（多 seed × 多 k × 多消息）

| 配置 | 采样+验签 |
|------|----------|
| 4 seed × k∈{2,4,8,16} × 30 msg | 480/480 通过 |
| 3 seed × k∈{2,4,8,16} × 25 msg | 300/300 通过 |
| k=4,8,16 × 200 msg | 600/600 通过 |

采样成功率 100%，平均耗时 k=16 时 <10ms（Python）。

### 4.2 攻击失效验证

| 攻击方式 | 结果 |
|---------|------|
| 原 rank-1 提取攻击（attack_vwz.py 核心逻辑） | 0 成功 |
| 固定 w2 解 w3 线性系统（n×m 方阵/超定） | 0 成功 |
| rank-2 分解推广攻击 | 未成功 |

### 4.3 切片秩检查

公钥切片秩全部为 2（非 rank-1），rank-1 近似重建误差巨大（~4×10⁵），rank-1 提取无效。

### 4.4 尺寸对比

| 参数 | 原实现 | 修复后 |
|------|--------|--------|
| m（w2/w3 维度） | k+1 | 2k+1 |
| n（target 长度） | 2k+1 | 2k+2 |
| 公钥 | n·m² | (2k+2)·(2k+1)² |
| k=8 公钥 | 578 字节 | 10,404 字节 |
| k=8 签名 | 68 字节 | 68 字节（2·(2k+1)·2） |
| k=16 公钥 | — | 74,052 字节 |
| k=16 签名 | — | 132 字节 |

签名尺寸 = 2·m·2 字节（m=2k+1），k=8 → 68B，k=16 → 132B（已按 m=2k+1 重新核算确认）。

## 5. 验证脚本

`security-assessment/fix/verify_vwz_fix_mixed2.py`

运行：`python3 security-assessment/fix/verify_vwz_fix_mixed2.py [seed]`

## 6. 安全性说明与限制

- 本方案是**实验性修复**，已验证破坏已知攻击（rank-1 提取、固定-w2 线性系统、rank-2 分解推广）
- 张量 preimage 问题的困难性基于 VWZ 论文假设；混合结构将其从 rank-1 退化情形恢复为一般双线性系统
- **未完成形式化安全性论证**，建议在学术级代码审查后评估是否纳入正式版本
- 需要 Rust 工具链才能移植到 `rust/vwz-sign-wasm/` 并编译验证

## 7. 后续工作

- [x] 移植到 Rust（tensor.rs / trapdoor.rs / preimage.rs / signature.rs）
- [x] 与官方 WASM verify() 对比验证
- [ ] 形式化安全性论证（双线性系统求解困难性）

## 8. Rust 移植与 WASM 验证结果（2026-08-16）

### 8.1 移植内容

在 `experimental/vwz-lg` 分支对 `rust/vwz-sign-wasm/` 完成修复移植：

| 文件 | 改动 |
|------|------|
| `src/tensor.rs` | `VwzTensor` → `MixedTensor`（la/lb/lc 三列表）；保留 `PubTensor`/`public_tensor_eval`/`public_tensor_eval_data`，维度改为 m=2k+1、n=2k+2 |
| `src/trapdoor.rs` | 新 `Trapdoor` 结构（x1, X2a/X2b/X3a/X3b, X2a⁻¹/X3a⁻¹, M2/M3, seed）；`generate_trapdoor` 生成四组可逆基变换；`build_public` 构建 rank-2 切片 |
| `src/preimage.rs` | 线性代数核心（vand/mat_vec/mat_t_vec/transpose/mat_mul/mat_inv/rref_and_ns/solve_linear）+ `solve_preimage_mixed`（Za/Zb 划分 → u3 零空间 → u2 方阵求解）；采样 RNG 用 `seed ^ target` 派生，保持确定性签名 |
| `src/hash_target.rs` | n 从 2k+1 → 2k+2 |
| `src/signature.rs` | 序列化长度校验、`estimate_sizes` 按 m=2k+1 更新；新增 3 个安全回归测试（rank-1 攻击失效 / 固定-w2 攻击失效 / 切片 rank-2） |
| `src/lib.rs` | 移除已失效模块声明（constants/structured/vwz_rank1，文件保留为历史参考，不再编译） |
| `src/bin/bench.rs` | 修正引用与尺寸核算 |

### 8.2 验证结果

**Rust 单元测试（release，32/32 通过）**：
- 合法签名验签：3 seed × k∈{2,4,8,16} × 25 msg = **300/300**
- 采样成功率：k∈{4,8,16} × 200 target 全成功
- **rank-1 提取攻击 0 伪造**（k∈{2,4,8} × 8 target）
- **固定 w2 解 w3 攻击 0 伪造**（k∈{2,4,8} × 8 target）
- 切片全部为 rank-2（非 rank-1 可分离）

**WASM 端到端（`node test-mixed.mjs`，313/313 通过）**：
- 经官方 WASM `verify()`：3 seed × k∈{2,4,8,16} × 25 msg = **300/300 接受**
- 篡改消息/篡改签名全部拒绝；`verify_batch` 全有效 + 单点篡改检测正确
- 序列化往返、尺寸（k=8 PK=10404B / sig=68B / N=18 / M=17）、确定性签名均符合预期

**原生性能（release）**：

| 操作 | k=8 | k=16 |
|------|-----|------|
| keygen | 0.45ms | 2.96ms |
| sign（含采样） | 0.088ms | 0.49ms |
| verify | 0.028ms | 0.18ms |
| 采样 | 0.082ms | 0.49ms |

**Python 修复脚本回归**：k∈{2,4,8,16} 验签 120/120、rank-1 攻击 0、fixed-w2 攻击 0（与移植前一致）。

## 9. 雪崩效应实验（2026-08-17）

脚本：`security-assessment/attack/vwz-avalanche-test.py`（k=8/seed=12345 的 Python oracle 与 Rust WASM `serialize_signature` 输出**字节级一致**，自检 PASS）。

### 9.1 测量方法

- 固定 keygen seed（424242），对 16 字节随机消息逐一翻转第 b 位（b<8·ntrials 取前 nbits 位）
- `target` 雪崩：翻转前后 `hash_to_sparse_target` 的**逐元素**变化比例
- 签名结构雪崩：翻转前后签名 `w2`、`w3` 的逐元素变化比例
- 对比：原始 rank-1 VWZ（n=2k+1, m=k+1，`solve_preimage_sparse`）vs 混合张量修复版（n=2k+2, m=2k+1，`solve_preimage_mixed`）

### 9.2 结果（逐元素变化比例）

| k | 指标 | 混合张量修复版 | 原始 rank-1 |
|---|------|----------------|-------------|
| 2 | target | 74.17% | 83.17% |
| 2 | w2 | 100.00% | 99.83% |
| 2 | w3 | **100.00%** | **89.50%（min 0%）** |
| 4 | target | 74.90% | 81.83% |
| 4 | w2 | 100.00% | 100.00% |
| 4 | w3 | **100.00%** | **98.96%（min 0%）** |
| 8 | target | 74.70% | 78.19% |
| 8 | w2 | 99.97% | 100.00% |
| 8 | w3 | 99.94% | 99.94% |
| 16 | target | 75.12% | 76.52% |
| 16 | w2 | 99.96% | 100.00% |
| 16 | w3 | 100.00% | 100.00% |

（trials：k=2/4 为 50-80 条消息 × 12 位；k=8 为 60×12；k=16 为 40×12。全部采样成功，无 sample-fail。）

### 9.3 关键发现

1. **target 雪崩**：两版相当（~75%）。target 是 Hamming weight=k+1 的稀疏向量，位置集合由 SHAKE-256 派生的 Fisher-Yates 重排决定，1 bit 消息翻转后位置与值均随机重排，逐元素差异比例稳定在 74-83%，符合稀疏随机向量预期。

2. **签名结构雪崩**：两版 w2 均接近 100%（签名主体强雪崩）。

3. **w3 结构差异（重要）**：
   - **原始 rank-1 版**：`w3` 是 `P3(X)=∏(X−λ_{i1,3})` 的系数，**仅由 target 零位/非零位分区（位置集合）决定，与具体值无关**。当 1 bit 翻转后位置集合恰好不变（仅值变化）时，w3 完全不变（diff=0%），实测 k=2/4 出现该退化，k=8/16 未出现（位置集合相撞概率随 n 增大迅速下降）。
   - **混合张量修复版**：`w3` 来自 u3 零空间向量（随机组合 + M3 混合），**对值变化全敏感**，任何消息翻转都导致 w3 完全变化（100%）。这是 rank-2 结构 + 随机化采样相对 rank-1 结构在雪崩性上的系统性改进。
