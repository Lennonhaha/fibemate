{
  "name": "@fibemate/pqc-kem",
  "version": "0.1.0",
  "description": "ML-KEM-768 (FIPS 203) - pure JS post-quantum KEM. No deps, no WASM, no NTT.",
  "keywords": [
    "ml-kem",
    "post-quantum",
    "fips-203",
    "kem"
  ],
  "license": "GPL-3.0-only",
  "main": "index.js",
  "files": [
    "index.js",
    "src/"
  ],
  "scripts": {
    "test": "node test/basic.test.js",
    "build:native": "node-gyp rebuild -C native",
    "build:native:release": "node-gyp rebuild -C native --release",
    "test:native": "node -e \"var a=require(\"./native/build/Release/mlkem.node\");var k=a.keygen();var e=a.encaps(k[0]);var d=a.decaps(e[0],k[1]);process.exit(Buffer.from(d).equals(e[1])?0:1);console.log(\"Native addon test PASS\")\"",
    "ci:build": "npm run build:native:release && npm run test:native"
  }
}
